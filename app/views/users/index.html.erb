<% provide(:title, 'All users') %>
<div class="container center top_padding">
  <h1>All users</h1>

  <%= will_paginate %>
  <ul class="users">
    <%= render @users %>
  </ul>
  <%= will_paginate %>
</div>
