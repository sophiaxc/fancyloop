<%= form_for(@project) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :title, placeholder: "Project title...", rows: 2 %>
  </div>
  <%= f.submit "Create", class: "btn btn-large btn-primary" %>
<% end %>
